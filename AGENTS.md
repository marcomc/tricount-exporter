# Project Agent Notes

- Use `make install` for a local runtime install and `make install-dev` before linting.
- The supported entry points are `tricount-exporter` and `python main.py`.
- Keep exports inside title-based folders under `outputs/`, and store the public key in `tricount-info.json`.
- Do not commit personal Tricount keys or generated exports.
